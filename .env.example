# Email Configuration for Contact Form
# Copy this file to .env.local and fill in your actual credentials

# Database Configuration
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require
# Note: DIRECT_URL is optional, only needed for migrations

# SMTP Server Settings (Outlook/Office365)
SMTP_HOST=smtp-mail.outlook.com
SMTP_PORT=587
SMTP_USER=microailabs@outlook.com
SMTP_PASSWORD=your_outlook_password_here

# Authentication
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_random_secret_here_minimum_32_characters

# Note: For security, never commit your .env.local file to git
# When deploying to Render or other platforms, add these as environment variables in their dashboard
