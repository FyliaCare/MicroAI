# Database Configuration (Neon PostgreSQL)
# For local development:
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require
# For production (Neon with connection pooling):
DIRECT_URL=postgresql://user:password@host:5432/database?sslmode=require

# Authentication (NextAuth)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_random_secret_here_minimum_32_characters_long

# Email Configuration (SMTP)
SMTP_HOST=smtp-mail.outlook.com
SMTP_PORT=587
SMTP_USER=microailabs@outlook.com
SMTP_PASSWORD=your_outlook_password_here
EMAIL_FROM=microailabs@outlook.com

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000

# GitHub Integration (Optional)
GITHUB_TOKEN=your_github_personal_access_token
GITHUB_WEBHOOK_SECRET=your_webhook_secret

# File Upload (Optional)
UPLOAD_MAX_SIZE=10485760
UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,application/pdf

# Node Environment
NODE_ENV=development

# IMPORTANT: 
# - Copy this file to .env.local for local development
# - Never commit .env.local to git (it's in .gitignore)
# - For Render deployment, add these variables in Render Dashboard
# - Use Neon PostgreSQL connection string for DATABASE_URL and DIRECT_URL
