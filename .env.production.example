# ===================================================================
# MICROAI SYSTEMS - PRODUCTION ENVIRONMENT VARIABLES (RENDER + NEON)
# ===================================================================
# Copy these to Render Dashboard → Environment Variables
# Get Neon connection strings from: https://console.neon.tech

# ------------------ DATABASE (NEON POSTGRESQL) ------------------
# Pooled connection for app (use connection pooler)
DATABASE_URL="postgresql://USER:PASSWORD@HOST-pooler.region.aws.neon.tech/DATABASE?sslmode=require"

# Direct connection for migrations (no pooler)
DIRECT_URL="postgresql://USER:PASSWORD@HOST.region.aws.neon.tech/DATABASE?sslmode=require"

# ------------------ AUTHENTICATION (NEXTAUTH) ------------------
# Your deployed Render URL
NEXTAUTH_URL="https://your-app-name.onrender.com"

# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-generated-32-character-secret-key-here"

# ------------------ API CONFIGURATION ------------------
NEXT_PUBLIC_API_URL="https://your-app-name.onrender.com"

# ------------------ NODE ENVIRONMENT ------------------
NODE_ENV="production"

# ------------------ EMAIL (OPTIONAL - SMTP) ------------------
# Gmail/Outlook SMTP for contact form and notifications
SMTP_HOST="smtp-mail.outlook.com"  # or smtp.gmail.com
SMTP_PORT="587"
SMTP_USER="your-email@outlook.com"
SMTP_PASSWORD="your-app-password"
EMAIL_FROM="your-email@outlook.com"

# ------------------ GITHUB INTEGRATION (OPTIONAL) ------------------
# For project import feature
GITHUB_TOKEN="ghp_your_github_personal_access_token"
GITHUB_WEBHOOK_SECRET="your_webhook_secret"

# ------------------ FILE UPLOADS (OPTIONAL) ------------------
UPLOAD_MAX_SIZE="10485760"  # 10MB in bytes
UPLOAD_ALLOWED_TYPES="image/jpeg,image/png,application/pdf"

# ===================================================================
# DEPLOYMENT CHECKLIST FOR RENDER
# ===================================================================
#
# 1. Create Neon Database
#    - Go to https://console.neon.tech
#    - Create new project
#    - Copy connection strings (pooled + direct)
#
# 2. Add Environment Variables in Render
#    - Dashboard → Your Service → Environment
#    - Add each variable above
#    - Click "Save Changes"
#
# 3. Build Settings (Already in render.yaml)
#    - Build Command: npm install && npx prisma generate && npm run build
#    - Start Command: npm start
#
# 4. Deploy
#    - Push to GitHub main branch
#    - Render auto-deploys
#    - Watch logs for success
#
# 5. Post-Deployment
#    - Run migrations: npx prisma migrate deploy
#    - Seed database: npm run db:seed
#    - Test health: https://your-app.onrender.com/api/health
#    - Create admin user (see docs/deployment/)
#
# ===================================================================
